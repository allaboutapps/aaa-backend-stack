<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="utf-8">
    <title>Renew password</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        .main {
            background-color: white;
        }

        .content {
            width: 300px;
            margin: 0 auto;
            padding-top: 40px;
        }

        .text {
            font-family: sans-serif;
            color: black;
            font-size: 22px;
        }

        .error {
            font-family: sans-serif;
            color: red;
            font-size: 14px;
        }

        .pwdlabel {
            font-size: 18px;
            color: #999999;
        }

        .pwdbox {
            background-color: transparent;
            color: white;
            outline: none;
            outline-style: none;
            border-top: none;
            border-left: none;
            border-right: none;
            border-bottom: solid black 1px;
            padding: 3px 0 3px 0;
            width: 200px;
            font-size: 18px;
        }

        .pwdbutton {
            font-size: 16px;
            text-align: center;
            display: inline-block;
            background-color: black;
            text-decoration: none;
            border: none;
            color: white;
            padding: 8px 24px;
            width: 200px;
        }

        .centerform {
            margin: auto;
        }
    </style>
</head>

<body class="main">
    <div>
        <div class="content">

            {{#unless success}}
            <h1 class="text">Choose Password</h1>
            {{/unless}}

            <div class="centerform">
                {{#if success}}
                <h1 class="text">{{{success}}}</h1>
                {{else}}
                <form action="/api/v1/auth/forgot-password/complete" method="post">

                    <span class="pwdlabel">Password</span>
                    <br />
                    <input type="password" name="password" class="pwdbox" size="30">
                    <br />
                    <br />
                    <span class="pwdlabel">Repeat Password</span>
                    <br />
                    <input type="password" name="passwordConfirmation" class="pwdbox" size="30">
                    <br />
                    <input type="hidden" name="token" value="{{{token}}}">
                    <br /> {{#if error}}
                    <h1 id="error" class="error">{{{error}}}</h1>
                    {{/if}}
                    <br>
                    <input type="submit" value="RESET PASSWORD" class="pwdbutton">
                </form>
                {{/if}}
            </div>
        </div>
    </div>

</body>

</html>